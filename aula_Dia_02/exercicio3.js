function verificalidade (idade){
    let = ""

    if (idade >= 18) {
        result = "maior de idade"
    } else {
        result = "menor de idade"
    }
return result
}

let idade = prompt("digite sua idade")

document.write(verificalidade(idade));
